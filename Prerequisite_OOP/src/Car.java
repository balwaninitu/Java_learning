public class Car {

    /* hp is the instance variable is belonging to every single object
    which get created and it will have special slot in the heap which will maintain
    data for every single object in heap, insyance variable is use to maintain
    state of object

     */
    int hp;
    Engine myEngine;





}
